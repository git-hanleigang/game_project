local main = function(machine)
    -- machine._node_reward:removeFromParent()

    -- package.loaded["CashOrConkReward"] = nil

    -- machine._node_reward = util_createView("CashOrConkReward",{machine = machine})
    -- machine:findChild("jianglilan_classic"):addChild(machine._node_reward)
    -- machine._node_reward:plalIdle()

    -- machine._node_reward:setCoinLabelAndCount(434234343)
    -- machine._node_reward:playAddCoins(-434234343)

    -- machine._node_reward:setCoinLabelAndCount(0)
    -- machine._node_reward:playAddCoins(434234343)
    -- do return end
    -- machine:showDFEndView(1,"4_0",2)
    -- local view = machine:createDFView("3_1")
    -- view:setLockClick(false)
    -- view:refreshStatus()
end

return main